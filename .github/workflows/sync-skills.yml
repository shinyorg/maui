name: Sync Skills to Skills Repository

on:
  push:
    branches:
      - main
    paths:
      - 'skills/**'
  workflow_dispatch:

jobs:
  sync-skills:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source repository
        uses: actions/checkout@v7
        with:
          path: source

      - name: Checkout skills repository
        uses: actions/checkout@v7
        with:
          repository: shinyorg/skills
          token: ${{ secrets.SKILLS_REPO_TOKEN }}
          path: skills-repo

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Sync skills directory
        run: |
          # Copy skills content to the shiny-maui plugin in the skills repo
          if [ -d "source/skills/shiny-maui-shell" ]; then
            # Remove existing skill directory to ensure clean sync
            rm -rf skills-repo/plugins/shiny-maui/skills/shiny-maui-shell
            mkdir -p skills-repo/plugins/shiny-maui/skills
            cp -r source/skills/shiny-maui-shell skills-repo/plugins/shiny-maui/skills/
          fi

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.SKILLS_REPO_TOKEN }}
          path: skills-repo
          commit-message: "Update shiny-maui-shell skill from Shiny MAUI Shell repository"
          title: "Update shiny-maui-shell skill"
          body: |
            This PR updates the shiny-maui-shell skill from the [Shiny MAUI Shell repository](https://github.com/shinyorg/ShinyMaui).

            **Source commit:** ${{ github.sha }}
            **Triggered by:** @${{ github.actor }}
            ---
            *This PR was automatically created by the [sync-skills workflow](https://github.com/shinyorg/ShinyMaui/actions/workflows/sync-skills.yml).*
          branch: update-shiny-maui-shell-skill
          delete-branch: true
          labels: |
            automated
            skills-update
